kind: enhancement
description: >
  The harness's viewer plan ladder now names the repository, branch and
  analysis insight count on its first rung and the workflow stage position,
  the first line of the stage task and the model on its middle rung, which
  re-renders on every turn with progress against the turn budget instead
  of one spinner for the whole agent turn; push tool calls name the branch
  they push to.
